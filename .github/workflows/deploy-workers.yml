name: Deploy Workers

on:
  push:
    branches: [main, develop]
    paths-ignore:
      - '**.md'
      - 'docs/**'
      - '.gitignore'
      - '.prettierignore'
      - 'LICENSE'

# 同一ブランチで新しいプッシュがあった場合、古いワークフローをキャンセル
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

# 最小権限の原則
permissions:
  contents: read
  deployments: write

env:
  BUN_VERSION: '1.3.8'

jobs:
  deploy:
    name: 🚀 Deploy to Cloudflare
    runs-on: ubuntu-latest
    timeout-minutes: 10

    environment:
      name: ${{ github.ref == 'refs/heads/main' && 'production' || 'preview' }}
      url: ${{ github.ref == 'refs/heads/main' && 'https://kenken-pose-est.online' || 'https://dev.kenken-pose-est.online' }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: ${{ env.BUN_VERSION }}

      - name: Install dependencies
        run: bun install --frozen-lockfile

      # Preview (develop ブランチ)
      - name: Build (Preview)
        if: github.ref == 'refs/heads/develop'
        run: bun run build
        env:
          VITE_CF_BEACON_TOKEN: ${{ secrets.VITE_CF_BEACON_TOKEN }}
          VITE_API_BASE_URL: https://api-dev.kenken-pose-est.online

      - name: Deploy (Preview)
        if: github.ref == 'refs/heads/develop'
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          command: deploy

      # Production (main ブランチ)
      - name: Build (Production)
        if: github.ref == 'refs/heads/main'
        run: bun run build
        env:
          VITE_CF_BEACON_TOKEN: ${{ secrets.VITE_CF_BEACON_TOKEN }}
          VITE_API_BASE_URL: https://api.kenken-pose-est.online

      - name: Deploy (Production)
        if: github.ref == 'refs/heads/main'
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          command: deploy --env production

      # デプロイ成功通知
      - name: Deployment Summary
        run: |
          echo "### 🎉 Deployment Successful!" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          if [ "${{ github.ref }}" == "refs/heads/main" ]; then
            echo "**Environment**: Production" >> $GITHUB_STEP_SUMMARY
            echo "**URL**: https://kenken-pose-est.online" >> $GITHUB_STEP_SUMMARY
          else
            echo "**Environment**: Preview" >> $GITHUB_STEP_SUMMARY
            echo "**URL**: https://dev.kenken-pose-est.online" >> $GITHUB_STEP_SUMMARY
          fi
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Built with**: Bun ${{ env.BUN_VERSION }}" >> $GITHUB_STEP_SUMMARY
