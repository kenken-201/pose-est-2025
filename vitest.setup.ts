import '@testing-library/jest-dom';
import { beforeAll, afterAll, afterEach } from 'vitest';

// グローバルなセットアップが必要な場合はここに記述
// 例: Mock Service Worker (MSW) の設定など

beforeAll(() => {
    // テスト開始前の処理
});

afterEach(() => {
    // 各テスト終了後の処理
});

afterAll(() => {
    // 全テスト終了後の処理
});
